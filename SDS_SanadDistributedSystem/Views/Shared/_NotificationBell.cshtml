
<style>
    .mdb-skin .navbar {
        background-color: #1B2838;
    }

    .noti-content {
        position: fixed;
        left: 100px;
        background: #e5e5e5;
        border-radius: 4px;
        top: 47px;
        width: 350px;
        display: none;
        border: 1px solid #9E988B;
    }

    ul#notiContent {
        max-height: 300px;
        overflow: auto;
        padding: 0px;
        margin: 0px;
        padding-left: 20px;
    }

        ul#notiContent li {
            margin: 3px;
            padding: 3px;
            background: #FFF;
        }

    .noti-top-arrow {
        border-color: transparent;
        border-bottom-color: #F5DEB3;
        border-style: dashed dashed solid;
        border-width: 0 8.5px 8.5px;
        position: absolute;
        left: 42px;
        top: -8px;
    }

    span.noti {
        color: #FFF;
        margin: 15px;
        position: fixed;
        left: 109px;
        top: 0pc;
        font-size: 18px;
        cursor: pointer;
    }

    span.count {
        position: relative;
        top: -3px;
    }
</style>



@if (Request.IsAuthenticated)
{

    <span class="noti fa fa-bell"><span class="badge badge-primary badge-pill count">
        @if (ViewBag.CountNotficationOffline != 0)
        {
             @ViewBag.CountNotficationOffline
        }
        </span>
    </span>
    <div class="noti-content">
        <div class="noti-top-arrow"></div>
        <ul id="notiContent" class="list-group"></ul>
    </div>
}